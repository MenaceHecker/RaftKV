# Point Grafana at the compose Prometheus automatically, so the dashboard
# works on first load rather than after a manual setup step.
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
