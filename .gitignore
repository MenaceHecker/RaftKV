# Binaries
#
# The leading slash matters: an unanchored "raftkv-server" would also match the
# cmd/raftkv-server source directory and silently keep the entrypoint out of
# the repository.
/bin/
/raftkv-server

# Test artifacts
*.test
*.out
coverage.html

# Runtime data produced by a local cluster
/data/
*.wal
*.snap

# Editor / OS
.DS_Store
.idea/
*.swp
